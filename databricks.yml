# Declarative Automation Bundle configuration, more info here: https://docs.databricks.com/en/dev-tools/bundles/settings.html
bundle:
  name: "finnhub"

include:
  - jobs/*.job.yml

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-5872019c-6930.cloud.databricks.com

  # The 'prod' target, used for production deployment.
  prod:
    mode: production
    workspace:
      host: https://dbc-5872019c-6930.cloud.databricks.com
      # We always use /Users/girianiruddha4@gmail.com for all resources to make sure we only have a single copy.
      root_path: /Users/girianiruddha4@gmail.com/.bundle/${bundle.name}/${bundle.target}
    run_as:
      # This runs as girianiruddha4@gmail.com in production. We could also use a service principal here,
      # see https://docs.databricks.com/dev-tools/bundles/permissions.html.
      user_name: girianiruddha4@gmail.com
